# Integrations

> Generated by GSD codebase mapper on 2026-01-02

## Authentication

### Supabase Auth
- **Primary auth system**
- Email/password, magic links
- JWT tokens with RLS policies
- Session management

### Firebase Auth
- **Phone authentication** for Hushh Agent
- OTP verification
- Used in: `src/hushh-agent/config/firebase.ts`

### Social Sign-In
- **Google Sign-In**: `src/services/authentication/googleSignIn.ts`
- **Apple Sign-In**: `src/services/authentication/appleSignIn.ts`
- Integrated with Supabase session

## AI Providers

### Google Gemini
- **Package**: `@google/genai`
- **Use Cases**:
  - Kai voice assistant
  - Chat completions
  - Profile inference (DOB, address)
  - Vision for resume analysis
- **Edge Functions**: `hushh-ai-chat`, `hushh-dob-inference`, `hushh-address-inference`

### OpenAI
- **Package**: `openai`
- **Use Cases**:
  - Investor profile generation
  - Chat completions
  - Content generation
- **Edge Functions**: `generate-investor-profile`

### MediaPipe
- **Package**: `@mediapipe/tasks-vision`
- **Use Case**: Emotion detection in Hushh Agent
- **Location**: `src/hushh-agent/services/mediapipeService.ts`

## Payment Processing

### Stripe
- **Edge Functions**: 
  - `chat-create-checkout` - Chat access payments
  - `onboarding-create-checkout` - Investor onboarding
  - `chat-verify-payment` - Payment verification
  - `onboarding-verify-payment`
- **Webhooks**: Payment confirmation

## Communication

### EmailJS
- **Package**: `@emailjs/browser`
- **Use Case**: Contact forms, notifications
- Client-side email sending

### Nodemailer (via Edge Functions)
- **Server-side emails**
- Used in: `sales-mailer`, `nda-signed-notification`
- Gmail API integration for some functions

### Gmail API
- **OAuth2 service account**
- Used in: `github-devops-notify`, `sales-mailer`
- **Location**: `supabase/functions/*/gmail.ts`

## Data & Storage

### Supabase Storage
- **File uploads**: Resumes, documents, assets
- **Buckets**: Created via migrations
- S3-compatible API

### Supabase Database
- **PostgreSQL**
- Row Level Security (RLS)
- Real-time subscriptions
- 50+ tables via migrations

## External APIs

### Country/State/City
- **Package**: `country-state-city`
- Location dropdowns in onboarding

### Phone Validation
- **Package**: `libphonenumber-js`
- International phone number parsing

### Stock Quotes
- **Edge Function**: `stock-quotes`
- Market data for investment features

### Calendar Integration
- **Calendly**: Embedded booking
- **Google Calendar API**: CEO calendar booking
- **Edge Function**: `ceo-calendar-booking`

## Identity Verification

### Stripe Identity
- **Edge Functions**:
  - `create-verification-session`
  - `identity-verification-webhook`
- Document verification for investors

## CI/CD Integrations

### Vercel
- **Frontend deployment**
- Serverless functions (api/)
- Preview deployments

### GitHub Actions
- **Workflows**: `.github/workflows/`
  - `pr-checks.yml` - PR validation
  - `pr-agent.yml` - Automated PR reviews
- **Danger**: `dangerfile.ts` for PR checks

### iOS/Android Builds
- **Capacitor**: Cross-platform builds
- **Scripts**: `ios-deploy.sh`, `android-playstore.sh`
- **TestFlight**: Automated uploads

## Cloud Services

### Google Cloud Run
- **Email Templates**: React Email rendering
- **OSINT Services**: Profile enrichment
- **Location**: `cloud-run/email-template-api/`

### Google Cloud Scheduler
- Token refresh automation
- **Script**: `setup-cloud-scheduler-token-refresh.sh`

## Integration Patterns

### Edge Function Pattern
```typescript
// supabase/functions/{name}/index.ts
import { corsHeaders } from '../_shared/cors.ts'

Deno.serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }
  // Function logic
})
```

### API Client Pattern
```typescript
// src/services/{feature}/apiClient.ts
import { supabase } from '@/lib/supabase'

export const fetchData = async () => {
  const { data, error } = await supabase.functions.invoke('function-name', {
    body: { ... }
  })
}
```

### Webhook Pattern
```typescript
// Edge function receives webhook
const signature = req.headers.get('stripe-signature')
const event = stripe.webhooks.constructEvent(body, signature, secret)
```

## Environment Variables Required

| Variable | Service |
|----------|---------|
| `VITE_SUPABASE_URL` | Supabase |
| `VITE_SUPABASE_ANON_KEY` | Supabase |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase (server) |
| `VITE_GOOGLE_AI_KEY` | Google Gemini |
| `VITE_OPENAI_API_KEY` | OpenAI |
| `STRIPE_SECRET_KEY` | Stripe |
| `STRIPE_WEBHOOK_SECRET` | Stripe Webhooks |
| `VITE_FIREBASE_*` | Firebase |
| `GMAIL_CLIENT_*` | Gmail API |
