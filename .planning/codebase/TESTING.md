# Testing

> Generated by GSD codebase mapper on 2026-01-02

## Testing Stack

| Type | Tool | Location |
|------|------|----------|
| Unit Tests | Vitest | `tests/` |
| E2E Tests | Playwright | `tests/` (e2e) |
| PR Checks | Danger | `dangerfile.ts` |

## Running Tests

### Unit Tests
```bash
# Run all tests
npm test

# Run tests in watch mode
npm run test:watch

# Run with UI
npm run test:ui
```

### E2E Tests
```bash
# Run all E2E tests
npm run test:e2e

# Run specific E2E tests
npm run test:e2e:deepsearch

# Run with UI
npm run test:e2e:ui

# Run in headed mode (visible browser)
npm run test:e2e:headed

# Debug mode
npm run test:e2e:debug

# View test report
npm run test:e2e:report
```

## Test Configuration

### Vitest Config (`vitest.config.ts`)
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    // Test configuration
  },
})
```

## Existing Test Files

| File | Purpose |
|------|---------|
| `tests/profileSearch.test.ts` | Profile search service tests |
| `tests/locationService.test.ts` | Location service tests |
| `tests/nda.test.ts` | NDA functionality tests |
| `tests/ndaNotification.test.ts` | NDA notification tests |
| `tests/ndaIntegration.test.ts` | NDA integration tests |
| `tests/step6LocationFlow.test.ts` | Onboarding step 6 location flow |

## Test Scripts

Located in `scripts/`:

| Script | Purpose |
|--------|---------|
| `test-hushh-profile-search.sh` | Test profile search edge function |
| `test-address-inference.sh` | Test address inference |
| `test-nda-notification.sh` | Test NDA notification |
| `test-resume-analysis-agent.sh` | Test resume analysis agent |
| `test-veo-api.sh` | Test video generation API |

## Writing Tests

### Unit Test Pattern
```typescript
import { describe, it, expect, beforeEach } from 'vitest';

describe('FeatureName', () => {
  beforeEach(() => {
    // Setup
  });

  it('should do expected behavior', async () => {
    // Arrange
    const input = { /* test data */ };
    
    // Act
    const result = await functionUnderTest(input);
    
    // Assert
    expect(result).toBeDefined();
    expect(result.field).toBe('expected');
  });

  it('should handle error cases', async () => {
    await expect(functionUnderTest(null))
      .rejects.toThrow('Expected error');
  });
});
```

### Mocking Supabase
```typescript
import { vi } from 'vitest';

vi.mock('@/lib/supabase', () => ({
  supabase: {
    from: vi.fn(() => ({
      select: vi.fn(() => ({
        eq: vi.fn(() => ({
          single: vi.fn(() => Promise.resolve({ data: mockData, error: null }))
        }))
      }))
    }))
  }
}));
```

### Testing Hooks
```typescript
import { renderHook, act } from '@testing-library/react-hooks';
import { useCustomHook } from './useCustomHook';

describe('useCustomHook', () => {
  it('should update state correctly', () => {
    const { result } = renderHook(() => useCustomHook());
    
    act(() => {
      result.current.updateValue('new value');
    });
    
    expect(result.current.value).toBe('new value');
  });
});
```

## E2E Testing with Playwright

### E2E Test Pattern
```typescript
import { test, expect } from '@playwright/test';

test.describe('Feature', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  test('user can complete action', async ({ page }) => {
    await page.click('[data-testid="button"]');
    await expect(page.locator('.result')).toBeVisible();
  });
});
```

## PR Automation

### Danger.js (`dangerfile.ts`)
Automated PR checks:
- Lint PR descriptions
- Check for required files
- Validate commit messages
- Flag large PRs
- Enforce code review policies

### GitHub Workflows

**`.github/workflows/pr-checks.yml`**
- Runs on PR creation/update
- Executes test suite
- Validates build

**`.github/workflows/pr-agent.yml`**
- Automated PR reviews
- AI-powered code analysis

## Build-Time Testing

Tests run as part of build:
```json
{
  "scripts": {
    "build": "vitest run && vite build && ..."
  }
}
```

This ensures:
- All tests pass before build
- Broken code doesn't get deployed
- CI/CD pipeline fails fast

## Coverage Considerations

### Critical Paths to Test
1. **Authentication flows** - Login, signup, session management
2. **Onboarding steps** - Data collection, validation
3. **Payment flows** - Checkout, verification
4. **AI integrations** - Profile generation, inference
5. **NDA signing** - Legal compliance

### Edge Function Testing
Test edge functions via shell scripts that:
1. Make HTTP requests to deployed functions
2. Validate response format
3. Check error handling

Example:
```bash
#!/bin/bash
# scripts/test-some-function.sh

curl -X POST "$SUPABASE_URL/functions/v1/function-name" \
  -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
  -H "Content-Type: application/json" \
  -d '{"param": "value"}'
```

## Testing Best Practices

1. **Test behavior, not implementation** - Focus on what the code does
2. **Use meaningful assertions** - Clear error messages
3. **Isolate tests** - No shared state between tests
4. **Mock external dependencies** - Supabase, APIs
5. **Test error paths** - Not just happy paths
6. **Keep tests fast** - Parallel execution
